# Persons Finder Requirements Traceability

This is the human-owned requirements and decision ledger. Product behavior remains planned; completed infrastructure spikes are labeled independently from product delivery.

## Status vocabulary

- `accepted`: human-approved direction; delivery evidence remains `planned` until produced.
- `rejected`: excluded alternative; absence/scope evidence remains `planned`.
- `deferred`: outside mandatory core until its activation gate is approved.
- `open`: must not be resolved by assumption or encoded as a final contract.
- `planned`, `implemented`, and `verified` describe delivery truth independently of decision status.

## Challenge requirements

| ID | README requirement | Decisions | Implementation evidence | Verification evidence |
|---|---|---|---|---|
| CR01 | Implement the REST API in Kotlin/Java-preferred technology. | D07, D08 | `[planned]` | `[planned]` |
| CR02 | `POST /persons` creates a person from name, job title, hobbies, and lat/lon location. | D01-D03, D06 | `[implemented]` | `[verified]` focused controller/application/domain/real-PostGIS checks |
| CR03 | Creation generates a short, quirky bio based on job and hobbies. | D19, D21, D22 | `[implemented]` deterministic assessment adapter, opt-in model-authored prose adapter, strict prose validation, and one-pass local grounding | `[verified offline]` focused policy/prose/composition/atomicity and captured application-owned request checks; `[verified live compatibility/calibration]` at the final 256-token ceiling, OpenAI smoke 3/3 and two fixed-corpus calibrations 24/24 were valid, with 12 distinct outputs within each corpus run |
| CR04 | The AI boundary may use a real model or a mock; architecture matters more than live credentials. | D19, D20 | `[implemented]` provider-neutral port with deterministic and generic remote adapters plus OpenAI/Gemini/Anthropic clients | `[verified offline]` all three captured application-owned provider request contracts; `[verified live compatibility/calibration]` OpenAI strict structured output passed 42 post-fix calls with zero request-boundary or harness errors; this is not a production reliability certification |
| CR05 | `PUT /persons/{id}/location` updates the person's location. | D01, D14-D18 | `[implemented]` | `[verified]` focused API and real-PostGIS transaction/concurrency checks |
| CR06 | `GET /persons/nearby` accepts query latitude, longitude, and radius. | D01, D04, D12 | `[implemented]` exact unversioned controller and indexed projection query | `[verified]` controller contract, real-PostGIS, and [Docker HTTP smoke](evidence/nearby-docker-smoke.md) |
| CR07 | Nearby returns persons including bio, sorted by distance. | D04, D12 | `[implemented]` spheroidal membership/order and UUID tie-break | `[verified]` exact-query/brute-force comparison and spatial edge fixtures |
| CR08 | AI collaboration is mandatory and `AI_LOG.md` records two or three truthful key interactions. | D27 | `[planned]` | `[planned]` |
| CR09 | Bio generation must resist prompt/instruction injection. | D05, D06, D19, D21, D22 | `[implemented for POST]` typed minimization, normalized source policy, strict structured provider prose, deterministic output validation, and one-pass opaque local composition | `[verified offline]` exact challenge attack and bypass variants, negative controls, zero-call/zero-write HTTP contract, hostile prose, nondeterministic prose properties, and real PostGIS checks |
| CR10 | `SECURITY.md` must explain sanitization, third-party PII risk, and a high-security banking design. | D21, D22 | `[implemented]` | `[verified]` documentation review against implemented controls and stated production gaps |
| CR11 | Code is clean and structured into controller/service/repository responsibilities. | D09, D19 | `[planned]` | `[planned]` |
| CR12 | Storage may vary; Docker Compose is preferred when a database is used. | D09-D12 | `[implemented]` Docker-first PostGIS/Flyway baseline | `[verified]` native arm64 lifecycle; native amd64 runtime evidence remains pending |
| CR13 | Required documentation is a run README, `AI_LOG.md`, and `SECURITY.md`. | D21, D22, D27 | `[implemented]` required files exist; AI case-study slots intentionally remain empty pending final truthful entries | `[partially verified]` README run instructions, dedicated remote-evaluation guidance, and security boundary reviewed |
| CR14 | Bonus: seed one million people and benchmark nearby. | D26 | `[planned - deferred]` | `[planned - deferred]` |
| CR15 | Bonus: apply DDD principles where they clarify the core model. | D14, D19 | `[planned]` | `[planned]` |
| CR16 | Bonus: unit-test the AI service despite nondeterministic-provider concerns. | D19, D20, D28 | `[implemented]` shared adapter conformance, property-based prose boundary, three provider-request, hostile-stub, opt-in live smoke tests, and a versioned aggregate evaluation harness | `[verified offline]` deterministic validation, distinct novel prose, captured application-owned request, corpus, statistics, call-budget, redaction, fake-provider harness phases, and content-free diversity diagnostics; `[verified live compatibility/calibration]` two post-fix smokes 6/6 and three one-pass 12-case calibrations 36/36 valid, with complete sanitized metering, schema-version-5 grounded metrics, and zero retries/top-ups |
| CR17 | Push the candidate's own solution to a public repository and submit its link; preserve assessment integrity. | D27, R09 | `[planned]` | `[planned]` |

## Final decision ledger

| ID | Status | Human decision | Implementation evidence | Verification evidence |
|---|---|---|---|---|
| D01 | accepted | README governs; implement only the three exact unversioned routes. | `[implemented]` | `[verified]` exact route contract and [Docker smoke](evidence/nearby-docker-smoke.md) |
| D02 | accepted | Public person and server observation IDs are UUIDs; `observationId` and `clientUpdateId` are distinct. | `[implemented for POST/PUT]` | `[verified for POST/PUT]` |
| D03 | accepted | POST accepts `name`, `jobTitle`, nonempty `hobbies`, and nested `location.latitude`/`longitude`; duplicate names are allowed. It appends one server-origin initial observation with no client update ID, using one server clock for person creation and observation capture/receipt. A 201 response sets relative `Location: /persons/{id}` and returns `id`, `name`, `jobTitle`, `hobbies`, `bio`, `createdAt`, and `lastKnownLocationAt`, never stored coordinates. | `[implemented]` | `[verified]` focused controller and real-PostGIS atomic-write checks |
| D04 | accepted | Nearby requires exactly one nonempty `lat`, `lon`, and `radius`, uses kilometres with `0 < radius <= 100`, includes the exact boundary and distance zero, and returns every match without a silent cap as a bare array (`[]` when empty). Items expose D03 public fields plus numeric one-decimal `distanceKm`; membership/order use unrounded spheroidal distance, then public UUID. | `[implemented]` | `[verified]` HTTP contract plus exact real-PostGIS comparison |
| D05 | accepted | Reject unknown body/query fields, alternate/empty/repeated query parameters, non-finite coordinates, latitude outside `[-90, 90]`, and longitude outside `[-180, 180]`. Return sanitized `application/problem+json` RFC 9457 bodies with required `type`, `title`, `status`, `detail`, and stable `code`, no `instance` or internals, and `{field, code}` violations sorted by field then code only for 400 `VALIDATION_FAILED`. Other codes are 404 `PERSON_NOT_FOUND`, 409 `IDEMPOTENCY_KEY_REUSED`, 422 `UNSAFE_BIO_INPUT`, 502 `BIO_GENERATION_INVALID`, and 503 `BIO_GENERATION_UNAVAILABLE`. Invalid/missing adapter configuration is a startup failure; cancellation propagates. | `[implemented]` | `[verified]` POST, PUT, and nearby contract checks |
| D06 | accepted | Canonicalize profile text by validating Unicode, trimming outer Unicode whitespace, applying NFC, and preserving case/internal whitespace/order; reject blank, malformed, control, line/paragraph-separator, and Unicode-format input except ZWJ/ZWNJ needed by legitimate scripts and emoji. Raw bounds are name 80, job title 80, 1-10 hobbies, and 60 per hobby, all in Unicode code points. Deduplicate exact canonical hobbies preserving first-input order. The selected name/job/grounding-hobby aggregate is at most 220 by those field bounds. Ordinary validation runs first and maps to 400. Only otherwise-valid job/hobby input matching the approved narrow deterministic instruction-manipulation or explicit secrets/identifier policy maps to 422; scan a separate NFKC security view with canonical decomposition, combining-mark and default-ignorable removal, do not scan names, and do not reject by fuzzy suspicion, toxicity, identity, or hobby topic. | `[implemented]` domain and request boundaries | `[verified]` focused bounds, normalization, bidi/format controls, diacritic/variation-selector/joiner and cross-field attack variants, benign controls, and controller checks |
| D07 | accepted | Use Spring Boot 4.1.0 rather than retaining the starter's unsupported 2.7.0 stack. The focused comparison passed its approved compatibility gate; container and real-PostGIS evidence remain separate gates. | `[implemented]` Boot 4.1.0 migration spike | `[verified]` clean wrapper test/build and focused runtime smoke |
| D08 | accepted | Pin Spring Boot 4.1.0, Kotlin 2.4.10, Gradle 9.6.1, Java 17, dependency-management plugin 1.1.7, and springdoc 3.0.3. The fallback is unnecessary unless later evidence falsifies this approved combination. | `[implemented]` build and wrapper pins | `[verified]` clean wrapper and container builds under the pinned versions |
| D09 | accepted | Use direct Spring JDBC, explicit PostGIS SQL, and Flyway as sole DDL owner; H2/JPA are not integration truth. | `[implemented]` direct JDBC, no H2/JPA dependency, and Flyway-owned V1-V3 migrations | `[verified]` fresh/idempotent migration and behavior against the pinned real PostGIS image |
| D10 | accepted | The Docker-first default must run natively on arm64 and amd64, bind the backend to loopback, keep the database internal without a host port, gate readiness on Flyway/database health, persist in a named volume, and require no host PostgreSQL, OIDC, or external AI credential. | `[implemented]` digest-pinned dual-architecture Dockerfiles, loopback app edge, internal-only database network, readiness, local secret file, and named volume | `[verified arm64; amd64 runtime pending]` native arm64 build/health/restart/log/teardown passed; pinned manifests contain amd64 and arm64 |
| D11 | accepted | Build the database image from official `postgres:17@sha256:a426e44bac0b759c95894d68e1a0ac03ecc20b619f498a91aae373bf06d8508d` plus `ghcr.io/pglayers/pgx-postgis:17@sha256:e2113e93a71f5f8fd272830c9c7c4843bc7d77cd0587f47e08679d52aa2a0c53`; keep the composition repository-owned and immutable. | `[implemented]` `Dockerfile.postgis` | `[verified arm64; amd64 runtime pending]` both manifests publish amd64/arm64; native arm64 ran PostgreSQL 17.10 and PostGIS 3.6.4 |
| D12 | accepted | Use PostGIS `geography(Point,4326)`, GiST, spheroidal `ST_DWithin` for boundary-inclusive membership, and spheroidal `ST_Distance` for unrounded ordering, then public UUID. Require real edge/query-plan evidence; Haversine is reference-only. | `[implemented]` V3 projection GiST and feature-owned JDBC query | `[verified]` indexed-versus-brute-force spheroidal comparison, poles, antimeridian, boundary, duplicates, ties, and representative plan |
| D13 | deferred | H3/S2 and road routing require measured falsification or a changed product metric. | `[planned - deferred]` | `[planned - deferred]` |
| D14 | accepted | Accepted observations are immutable normal-operation history; a one-row-per-person last-known projection is transactionally maintained and rebuildable. Each row has one canonical geographic-point source of truth, never independently divergent coordinate/geography values; D16 owns the representation/algorithm. | `[implemented]` V2 append/projection schema and transactional JDBC services | `[verified]` real-PostGIS schema, rollback, replay, late-event, rebuild, move-out/reentry, stale-current-location, and nearby-consumer checks |
| D15 | accepted | PUT always requires latitude/longitude; `capturedAt` and `clientUpdateId` appear together or neither. The no-key form is one new-project contract, not a compatibility alias: under a row lock, a request matching the current winning canonical point returns that observation without appending; otherwise it appends. “Immediate” means no intervening winning coordinate change, not a time window. Reusing `(personId, clientUpdateId)` with identical canonical point and normalized capture time returns the original observation; different content is 409. A new client ID may append the same coordinate. Late history cannot rewind the greatest `(capturedAt, receivedAt, observationId)` winner. | `[implemented]` transactional application/JDBC path | `[verified]` real-PostGIS replay, conflict, late-event, concurrent no-key, concurrent distinct-key winner, and concurrent conflicting-key checks |
| D16 | accepted | Persist public/server IDs as native PostgreSQL UUIDs; generate person/observation UUIDv4 and accept non-nil standard-variant client UUIDs, render canonical lowercase, and compare unsigned RFC network-order bytes. Canonical coordinates reject non-finite/out-of-range values, map negative zero to positive zero, longitude `+180` to `-180`, and pole longitude to zero without other rounding. Equality is exact after canonicalization; rows use only `geography(Point,4326)` (longitude X, latitude Y) as coordinate truth. | `[implemented]` domain UUID/coordinate types and V2 geography schema | `[verified]` domain canonicalization plus real schema/replay inspection |
| D17 | accepted | Client `capturedAt` uses restricted RFC 3339 with explicit `T`, uppercase `Z` or numeric offset, zero-to-three fractional digits, no leap second or finer precision, UTC-millisecond normalization, at most five minutes future skew, and no oldest bound. Server times are persisted at PostgreSQL microsecond precision; UTC API responses use `yyyy-MM-dd'T'HH:mm:ss.SSS'Z'`. `lastKnownLocationAt` is the winning observation's `capturedAt`. PUT returns exactly person/observation IDs and times plus current last-known observation ID/time, with no coordinates, client ID, or no-op flag; replay returns original observation plus current last-known fields. | `[implemented]` | `[verified]` focused timestamp, API representation, future-skew, and real winner checks |
| D18 | deferred | Retention, erasure, purge repair, post-purge replay, restore, and advanced receipt/crypto work need separate approval. | `[planned - deferred]` | `[planned - deferred]` |
| D19 | accepted | Use an application-owned `BioGenerator` and application-owned request/result/failure types. The deterministic credential-free adapter is the test/assessment-local default; unknown configuration fails startup/readiness, runtime failures normalize, and silent fallback is forbidden. | `[implemented]` provider-neutral port and deterministic infrastructure adapter | `[verified]` focused configuration, failure, and atomicity checks |
| D20 | accepted | Add one generic opt-in network/private `RemoteBioGenerator` behind the existing provider-neutral `BioGenerator` port. It is configured with exactly one active provider and model per runtime and delegates authentication, wire format, and response extraction to a small infrastructure `ModelProviderClient` boundary; OpenAI, Gemini, or another provider plugs into that boundary rather than creating another application adapter. This is configuration-based selection, not runtime auto-routing. Deterministic generation remains the assessment-local/test default. Provider-specific credentials come only from environment or mounted secrets. Every outbound request contains only the D21 typed allowlist and requests one structured model-authored prose template. Existing local validation/rendering, normalized failures, metadata-only logging, failure atomicity, and the ban on cross-provider or deterministic fallback remain mandatory. | `[implemented]` generic prose adapter, bounded JDK transport, configuration gate, OpenAI/Gemini/Anthropic structured-output clients, and an OpenAI-only exact-placeholder schema constraint | `[verified offline]` all three focused provider contracts; `[verified live compatibility/calibration]` 42/42 post-fix OpenAI calls valid with zero boundary/harness errors, including 27/27 at the final limits |
| D21 | accepted | Generator context is limited to literal `{{NAME}}`, deployment constants `en-NZ`/`NZ`, required broad job category plus mapping version, nonempty broad interests plus mapping version, optional closed macro-region, and `quirky` tone. Raw name/job/employer/role/hobbies/place/coordinates/identity/tokens and person-derived transport metadata cannot enter `BioTemplateRequest`. Remote output is exactly one size-bounded, strictly parsed `bio_template` string authored by the selected model. The calibrated provider output-token allowance is 256; the application independently requires one to three safe sentences, printable ASCII, at most 512 non-placeholder code points, exactly one each of `{{NAME}}`, `{{JOB}}`, and `{{HOBBY}}`, no unknown token or forbidden region, then composes validated source values once as opaque segments and revalidates grounding and the final 732-code-point contract. The final allowance consists of 512 authored points plus the existing 220-point maximum for selected source values and does not relax any other security gate. | `[implemented]` typed request, model-authored prose, deterministic validation, trusted local composer, and public-schema alignment | `[verified offline]` allowlisted application-owned request, distinct non-catalog prose, strict structured output, placeholder/sentence/policy matrix, exact 512/732 bounds, one-pass adversarial composition, and failure atomicity; `[verified live at final limits]` smoke 3/3 and two one-pass 12-case calibrations 24/24 valid, with maximum 59 output tokens, 188 authored code points, and 408 final code points under maximum-source grounding |
| D22 | accepted | `SafeJobCode` `job-v1` is exactly technology_engineering, healthcare, education_research, creative_media, business_operations, finance_legal, sales_service, trades_manufacturing, hospitality_retail, public_community_service, student, other. `SafeInterestCode` `interest-v1` is exactly outdoors_nature, sports_fitness, arts_crafts, music, reading_writing, food_drink, games_puzzles, technology_making, gardening, travel, other. Reviewed aliases use deterministic case-normalized exact whole-value matching only. Hobbies normalize/deduplicate in order; outbound codes deduplicate in order. Final grounding selects the first exactly mapped original hobby, otherwise the first benign unmatched hobby. Sensitive/rare topics have no specific alias and map to other without categorical rejection. The both-`other` remote path is deliberately generic; exact source grounding remains local. | `[implemented]` | `[verified]` exhaustive catalogs/aliases, exact/no-fuzzy matching, ordering/deduplication, selected-hobby rule, both-other, sensitive-topic fallback, and source-egress checks |
| D23 | deferred | Optional `macro_region` is default-off and omitted. Any separately approved future use is restricted to `North Island`/`South Island`, omitted outside or for unknown NZ coverage, and cannot surface in a bio unless explicitly requested. | `[planned - deferred]` | `[planned - deferred]` |
| D24 | accepted | Preserve compatibility seams for versioned boundaries, rebuildable area/count projections, privacy-safe aggregate maps, normalized name search, authorized last-known/history access, OIDC ownership, sharing grants, and Android foreground-location updates. Always label location `last known`, never `current`. | `[planned]` | `[planned]` |
| D25 | deferred | Auth, sharing, mobile, admin, search, map, and history implementation are outside mandatory core. | `[planned - deferred]` | `[planned - deferred]` |
| D26 | deferred | The one-million-person benchmark follows mandatory correctness and uses only project evidence. | `[planned - deferred]` | `[planned - deferred]` |
| D27 | accepted | Use concise stable agent rules, this single requirements/decision ledger, exactly three empty unlabeled AI case-study slots, ignored local journal/handoff material, and at most two schema-validated read-only reviewers. Preserve truthful evidence only, never copy another candidate's code, require explicit approval before commit/push, and add automation only after real commands pass. | `[planned]` | `[planned]` |
| D28 | accepted | Provider retention, abuse monitoring, human review, and product-improvement use, as applicable, are approved only for the fixed synthetic fixtures and fixed application-owned prompt/schema used by an explicitly authorized run. This does not assert logging is disabled and does not approve customer-derived or production data. The owner authorized the OpenAI investigation and necessary fixed reruns up to a cumulative USD 50 usage ceiling. Runs remain sequential and unpaced, never retry, top up, pool providers, or retain prompt/response content. The separately designed 12 cases x 38 repetitions = 456 reliability protocol was not executed for limit calibration. | `[implemented]` provider-specific authorization gates, sanitized request/response/usage/prose metrics, durable ignored checkpoints, exact call-budget enforcement, and explicit pacing configuration | `[verified live compatibility/calibration]` one three-call failure smoke exposed duplicate placeholders; after the schema fix, two three-call smokes and three one-pass 12-case calibrations completed 42/42 valid with no retry/top-up, including 27/27 at the final 256-token ceiling; all 45 calls, including the failed smoke, had an estimated cumulative cost of USD 0.024744; `[not executed]` 456-call reliability protocol |
| R01 | rejected | Versioned routes and compatibility aliases. | `[planned - excluded]` | `[planned]` |
| R02 | rejected | Enumerable numeric public identifiers. | `[planned - excluded]` | `[planned]` |
| R03 | rejected | Keeping the starter runtime by inertia or selecting a new major solely because it is newest. | `[planned - excluded]` | `[planned]` |
| R04 | rejected | JPA/Hibernate or H2 as spatial integration truth. | `[planned - excluded]` | `[planned]` |
| R05 | rejected | Host PostgreSQL or amd64 emulation as the evaluator default. | `[planned - excluded]` | `[planned]` |
| R06 | rejected | Application or unindexed SQL Haversine as production nearby search. | `[planned - excluded]` | `[planned]` |
| R07 | rejected | A mutable-only location row or latest-from-history work on every nearby query. | `[planned - excluded]` | `[planned]` |
| R08 | rejected | Direct provider coupling, raw-input egress, network evaluator default, or silent provider fallback. | `[implemented - excluded]` | `[verified]` typed application-owned outbound request, explicit remote runtime, and configuration/provider tests |
| R09 | rejected | Overlapping planning docs, decorative agents, fabricated evidence, TODO-only scripts, or placeholder-green CI. | `[planned - excluded]` | `[planned]` |
| R10 | rejected | Provider-visible raw source text, or local interpolation that rescans or reinterprets inserted source values. | `[implemented - excluded]` | `[verified]` typed egress request plus parsed one-pass opaque composition |

## Open-decision closure gates

| ID | Remains unresolved | Pass gate before dependent implementation | Implementation evidence | Verification evidence |
|---|---|---|---|---|
| — | No mandatory-core decision remains open for POST/PUT. | New behavior requires a new human decision rather than inference from this row. |  |  |

## Rejected alternatives and strongest arguments

| Decision | Rejected alternative | Strongest argument in its favor | Why it remains rejected |
|---|---|---|---|
| R01 | Versioned routes or aliases | Easier future client migration and API evolution. | Unneeded surface conflicts with the exact challenge contract. |
| R02 | Numeric public IDs | Smaller indexes and simpler starter compatibility. | They are enumerable and conflict with the approved public contract. |
| R03 | Keep the starter runtime | Lowest immediate migration effort. | Runtime selection must follow support and compatibility evidence. |
| R03 | Upgrade without a spike | Avoids disposable spike work if migration is inevitable. | It would hide dependency and toolchain risk. |
| R04 | JPA/Hibernate Spatial | Familiar mapping, repository, and transaction abstractions. | Exact spatial SQL and replay/locking behavior are clearer through JDBC. |
| R04 | H2 integration tests | Fast, credential-free local startup. | They do not verify PostGIS geography, GiST, or migrations. |
| R05 | Host PostgreSQL | Low overhead for developers who already run it. | It introduces evaluator and version drift. |
| R05 | amd64 image under emulation | Uses upstream-maintained packaging without owning an image. | Native arm64 is required and evaluator emulation is unknown. |
| R06 | Application Haversine | Transparent math and database portability. | It loads/scans serving rows and duplicates metric semantics. |
| R06 | Unindexed SQL Haversine | Keeps computation near the data without an extension. | It still performs unindexed per-row distance work. |
| R07 | One mutable coordinate row | Minimal schema and transaction logic. | It destroys required accepted history. |
| R07 | Query latest history each time | Avoids projection maintenance. | Nearby repeatedly scans/orders an unbounded trail. |
| R08 | Direct provider SDK | Fastest demonstration against one live model. | Vendor concerns would leak into application contracts. |
| R08 | Raw job/hobby prompt context | More specific model output. | It violates the approved minimization boundary. |
| R08 | Network-backed evaluator default | Demonstrates live nondeterministic behavior. | It sacrifices offline reproducibility and needs network/credentials. |
| R08 | Silent provider fallback | Higher apparent availability. | It conceals outages and makes behavior nondeterministic. |
| R09 | Separate requirements and decision docs | Clearer ownership as documentation grows. | One concise combined ledger is sufficient at assessment scale. |
| R09 | Larger reviewer/orchestration fleet | More perspectives and richer records. | It risks decorative process without proportional evidence. |
| R09 | Fabricated case-study evidence | Synthetic examples can make a sparse narrative look clearer and more complete. | Presenting invented material as actual evidence is deceptive; case studies must remain empty until truthful interactions exist. |
| R09 | Placeholder CI or TODO-only scripts | Makes intended commands visible early. | A green placeholder is false evidence; automation follows known-good commands. |
| R10 | Provider-visible raw source text or ordinary string/regex replacement | It could produce richer remote personalization with less local composition code. | It violates minimization or permits placeholder-looking source values to be interpreted twice; the approved core sends only broad codes and uses parsed one-pass local grounding. |

## Deferred extensions

| ID | Deferred scope | Activation gate | Evidence |
|---|---|---|---|
| D13 | H3/S2 candidate grids or road routing. | PostGIS is falsified by project measurements, or the product metric changes. | `[planned - deferred]` |
| D18 | Retention, erasure, purge repair, post-purge replay, restore, receipts, fingerprints, and crypto lifecycle. | Separate lifecycle, legal, and operational approval. | `[planned - deferred]` |
| D23 | NZ macro-region context. | Approved boundaries, coverage, disclosure rule, and tests. | `[planned - deferred]` |
| D25 | Auth/OIDC, sharing, Android, admin boundaries/counts/maps, name search, and authorized history. | Separate product, privacy, and authorization scope. | `[planned - deferred]` |
| D26 | One-million-person benchmark. | Mandatory correctness and an approved benchmark environment. | `[planned - deferred]` |

## Future verification matrix

| Evidence ID | Covers | Planned executable or artifact evidence | Binary pass condition | Result / link |
|---|---|---|---|---|
| E01 | D01, R01 | HTTP route/mapping contract checks. | Only the three approved routes map; aliases do not. | `[verified]` focused controller contracts and [default-stack smoke](evidence/nearby-docker-smoke.md) |
| E02 | D02-D04, R02 | API and persistence contract checks. | UUID/public-field/privacy/radius/order/rounding behavior exactly matches the ledger. | `[verified]` POST, PUT, and nearby |
| E03 | D05, D06 | Validation and sanitized Problem Details boundary checks. | Every approved boundary maps to the approved status/code without leaking values or internals. | `[verified]` POST, PUT, and nearby |
| E04 | D07, D08, R03 | Clean wrapper compatibility spike under recorded exact pins. | Accepted runtime compiles and focused checks pass, or the spike stops for human fallback selection. | `[verified]` Boot 4.1.0 `clean test`, `clean build`, application startup, existing-route smoke, and OpenAPI smoke passed; human approved the migration. |
| E05 | D09, R04 | Fresh/reused PostGIS migration and schema inspection. | Flyway alone owns DDL and repeat startup preserves migration integrity. | `[verified]` fresh V1-V3 migration succeeded; validation passed; repeated `migrate()` in the same application context executed zero migrations; the isolated Compose verification retained person/location state across a database-and-application restart on the same named volume. |
| E06 | D10, D11, R05 | Native arm64/amd64 manifest, build, health, networking, and persistence evidence. | Both architectures run without emulation; DB remains internal and volume behavior is explicit. | `[partially verified]` native arm64 passed; app bound only to `127.0.0.1:8080`, DB had no host binding, cluster ID survived restart, teardown retained the named volume, and all pins expose amd64/arm64. Native amd64 execution remains pending. |
| E07 | D12, D13, R06 | Real PostGIS edge fixtures and representative query plans. | Boundary, tie, antimeridian, pole, spheroidal order, and expected GiST behavior pass. | `[verified]` indexed query matched the brute-force spheroidal query; representative aarch64 plan used the projection GiST and is recorded in `docs/evidence/nearby-query-plan.txt` without a performance conclusion. |
| E08 | D14-D18, R07 | Real-PostGIS transaction, replay, concurrency, late-event, rollback, and schema checks. | Observation/projection state and returned winners match the approved comparator in every case. | `[verified]` POST/PUT regression suite, distinct/conflicting concurrency, move-out/reentry with stale retained history, stale current-location visibility, projection rebuild, and nearby-consumer equivalence |
| E09 | D19, D20, D28, R08 | Deterministic-adapter, configuration-failure, failure-atomicity, dependency-boundary, and opt-in live-evaluation checks. | Normal checks use no external model; no vendor leakage, fallback, partial state, retry, or unbudgeted live call occurs. Every live run remains synthetic-only, explicitly authorized, fixed-budget, and fail-closed; its sanitized evidence must survive pass, failure, or interruption without retaining content. | `[verified offline]` local and generic remote adapters plus OpenAI/Gemini/Anthropic captured application-owned request and hostile-stub tests pass; exact planning, property classification, sanitized reporting, checkpoints, and no-retry behavior pass. `[verified live compatibility/calibration]` two post-fix OpenAI smokes and three one-pass 12-case calibrations completed 42/42 valid with exactly 42 sends, zero retries/top-ups, complete evidence, and no boundary/harness error; the final 27 used the calibrated 256-token ceiling, and schema version 5 accounted for all 12 maximum-source grounded measurements. The 456-call reliability protocol was not executed. |
| E10 | D21-D23, R10 | Typed-context, exact-alias, strict structured prose, one-pass composition, and final-bio boundary checks. | Only approved context enters a generator request; any remote prose must pass deterministic placeholder, policy, one-to-three-sentence, 512-code-point authored, maximum-grounding, and 732-code-point final checks before locally grounded source is inserted once as opaque data; the provider output allowance is 256 tokens. | `[verified offline]` typed allowlist, complete application-owned request, distinct novel prose, exhaustive catalogs/aliases, malformed/unsafe-output rejection, placeholder/sentence/policy matrix, exact bounds and both public API schema `maxLength` declarations, adversarial composer cases, and transaction checks; `[verified live at final limits]` exact request configuration and all structural gates passed for 3/3 smoke plus two 12/12 corpus calibrations, with the latest report measuring 12/12 maximum-source grounded outputs |
| E11 | D24, D25 | `[planned]` Route/dependency/schema/diff scope review. | No deferred product implementation enters mandatory core. | |
| E12 | D26 | `[planned - deferred]` Reproducible seed, plans, latency distribution, memory, and write-amplification evidence. | Results meet a separately approved benchmark target on a declared environment. | |
| E13 | D27, R09 | `[planned]` Traceability, repository-status, ignore, AI-evidence, and configuration-schema review. | Harness artifacts are truthful, local material is untracked, and AI configuration is schema-valid. | |
