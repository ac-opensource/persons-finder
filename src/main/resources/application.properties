spring.application.name=persons-finder
persons.bio.generator=${PERSONS_BIO_GENERATOR:deterministic}
persons.runtime.mode=${PERSONS_RUNTIME_MODE:assessment-local}
persons.bio.remote.provider=${PERSONS_BIO_REMOTE_PROVIDER:}
persons.bio.remote.model=${PERSONS_BIO_REMOTE_MODEL:}
persons.bio.remote.timeout=${PERSONS_BIO_REMOTE_TIMEOUT:10s}
persons.bio.remote.openai.api-key=${OPENAI_API_KEY:}
persons.bio.remote.gemini.api-key=${GEMINI_API_KEY:}
persons.bio.remote.anthropic.api-key=${ANTHROPIC_API_KEY:}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.config.import=${SPRING_CONFIG_IMPORT:optional:configtree:/run/secrets/}
spring.flyway.clean-disabled=true
spring.jackson.deserialization.fail-on-unknown-properties=true
spring.main.banner-mode=off

management.endpoint.health.probes.enabled=true
management.endpoint.health.group.readiness.include=readinessState,db
management.endpoint.health.show-details=never
management.endpoints.web.exposure.include=health

server.error.include-binding-errors=never
server.error.include-message=never
server.error.include-stacktrace=never
server.shutdown=graceful
